	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* CLAIM lofree */
;
		;
		;
		;
		
	case 5: // STATE 13
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc9 */

	case 6: // STATE 2
		;
		now.pc[9] = trpt->bup.oval;
		;
		goto R999;

	case 7: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 9: // STATE 7
		;
		now.pc[9] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 11: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[9] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 14: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc8 */

	case 15: // STATE 2
		;
		now.pc[8] = trpt->bup.oval;
		;
		goto R999;

	case 16: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 18: // STATE 7
		;
		now.pc[8] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 20: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[8] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 23: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc7 */

	case 24: // STATE 2
		;
		now.pc[7] = trpt->bup.oval;
		;
		goto R999;

	case 25: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 27: // STATE 7
		;
		now.pc[7] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 29: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[7] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 32: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc6 */

	case 33: // STATE 2
		;
		now.pc[6] = trpt->bup.oval;
		;
		goto R999;

	case 34: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 36: // STATE 7
		;
		now.pc[6] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 38: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[6] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 41: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc5 */

	case 42: // STATE 2
		;
		now.pc[5] = trpt->bup.oval;
		;
		goto R999;

	case 43: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 45: // STATE 7
		;
		now.pc[5] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 47: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[5] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 50: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc4 */

	case 51: // STATE 2
		;
		now.pc[4] = trpt->bup.oval;
		;
		goto R999;

	case 52: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 54: // STATE 7
		;
		now.pc[4] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 56: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[4] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 59: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc3 */

	case 60: // STATE 2
		;
		now.pc[3] = trpt->bup.oval;
		;
		goto R999;

	case 61: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 63: // STATE 7
		;
		now.pc[3] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 65: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[3] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 68: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc2 */

	case 69: // STATE 2
		;
		now.pc[2] = trpt->bup.oval;
		;
		goto R999;

	case 70: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 72: // STATE 7
		;
		now.pc[2] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 74: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[2] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 77: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc1 */

	case 78: // STATE 2
		;
		now.pc[1] = trpt->bup.oval;
		;
		goto R999;

	case 79: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 81: // STATE 7
		;
		now.pc[1] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 83: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[1] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 86: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC proc0 */

	case 87: // STATE 2
		;
		now.pc[0] = trpt->bup.oval;
		;
		goto R999;

	case 88: // STATE 3
		;
		_m = unsend(now.queue);
		;
		goto R999;
;
		;
		
	case 90: // STATE 7
		;
		now.pc[0] = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 92: // STATE 12
		;
		now.cnt = trpt->bup.ovals[2];
		now.pc[0] = trpt->bup.ovals[1];
		XX = 1;
		unrecv(now.queue, XX-1, 0, trpt->bup.ovals[0], 1);
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		;
		;
		
	case 95: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;
	}

